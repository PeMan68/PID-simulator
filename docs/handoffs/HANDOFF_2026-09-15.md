# HANDOFF 2026-09-15 — Överlämning till nästa session (CC eller PM)

**Detta dokument ersätter `docs/handoffs/HANDOFF_2026-09-10.md`**, som vid det här laget
är en release och flera avslutade uppdrag ur fas. Läs det gamla dokumentet bara om du
behöver historisk detalj om arbetet fram till v1.5.1 — allt aktuellt finns här.

Du är antingen **Claude Code (CC)**, utvecklare i PID Simulator-projektet, eller läses av
**PM** (Microsoft Copilot) i en ny session. Läs hela detta dokument innan du analyserar
eller ändrar projektet. Fatta inga produktbeslut och påbörja inget nytt arbete innan PO
eller PM har lämnat ett konkret uppdrag.

---

## 1. Roller och beslutsmandat

- **Per Manholm** är produktägare, **PO**.
- **Microsoft Copilot** är projektledare, **PM**.
- **Claude Code** är utvecklare, **CC**.
- PO och PM fattar produktbeslut och pedagogiska beslut.
- CC analyserar, implementerar, testar och dokumenterar enligt ett konkret uppdrag.
- CC får inte själv välja vad som ska publiceras eller utvecklas härnäst.
- **Denna session:** PO arbetade direkt med CC i en interaktiv session (utan att PM var
  inkopplad), gav uppdrag löpande och fattade flera mindre beslut på plats (se avsnitt 5
  och 7). Samma mönster som redan etablerats i RELEASE-v1.5.0.

---

## 2. Produktvision

PID Simulator är en ren webbapp för undervisning och självstudier inom reglerteknik.

Appen ska främst stödja:
- lärarledda demonstrationer
- fler och snabbare experiment än fysiska laborationer medger
- självstudier genom lärstigar
- fördjupning inom reglerteknik
- träning i PID-tuning
- reglertekniska fenomen som är svåra att visa i fysisk labb

Simulatorn ska **inte** täcka hela kursens innehåll om mätdon och styrdon. Fokus är
regulatorn och reglerloopen:

```
process → mätdon → regulator → styrdon → process
```

Lärstigarna ska följa kursens veckoprogression och vara indelade i lämpliga
undervisningsportioner.

Nivåprogression (badge, nivånamn, grafisk mätare, inga synliga XP-tal) är aktiverad i
produktion sedan v1.5.0. Den mäter aktivitet/progression, är **inte ett betyg eller ett
mått på yrkeskompetens**, och sparas per webbläsare/enhet (inte per konto). Oförändrat
sedan förra handoffen.

---

## 3. Gitflow och livscykel

```
feature/<uppdrags-id>-<kort-namn>  →  develop
bugfix/ÅÅÅÅ-NNN                    →  develop
release/<version>                  →  main  →  tillbaka till develop
hotfix/<version>-<kort-namn>       →  main  →  tillbaka till develop
```

Regler (oförändrade):
- Ett uppdrag per feature/bugfix-branch.
- Feature/bugfix-branches mergeas aldrig direkt till `main`.
- Direkta commits till `main` ska inte göras.
- `main` ska innehålla stabil undervisningsversion.
- `develop` ska innehålla fortsatt utveckling.
- Officiell GitHub Pages-URL publicerar uteslutande från `main`
  (`deploy-pid-simulator.yml`, triggar bara på push till `main`).
- Release till `main` sker endast efter PO/PM-beslut.
- **Ingen cherry-pick eller selektiv release används.** En release tar med hela `develop`.
  **Praktisk konsekvens denna gång:** releasen av v1.5.2 (avsnitt 5) tog med sig HELA
  `develop`, inklusive FEAT-037 (reglerstrategier-övningarna, se avsnitt 8) som PO
  fortfarande höll på att granska/redigera för hand. Det är harmlöst för den publicerade
  sajten (FEAT-037 rör bara `docs/exercises/*.md`, aldrig `apps/app/`, och
  `deploy-pid-simulator.yml` bygger uteslutande `dist/prod` från `catalog.prod.json` — se
  `tests/lib/prod-content-set.mjs`), men det betyder att `main`s gitthistorik nu
  innehåller ett dokument som PO fortfarande aktivt redigerar. Nästa release tar återigen
  med allt som då finns i `develop`.
- **Feature-/bugfix-brancher raderas lokalt OCH på origin** efter lyckad merge.

**Ny lärdom denna session — releasechecklista saknar en versionskontroll:**
`apps/app/app.js` har en hårdkodad `APP_VERSION`-konstant (visas i appens sidfot) som
INTE ingår i `CHANGELOG.md`/`README.md`-uppdateringen som annars görs vid release (se
bugg 2026-015, avsnitt 10). Kom ihåg att bumpa den vid NÄSTA release — PROD visar just nu
"v1.5.1" trots att v1.5.2 är publicerad.

---

## 4. Aktuell git-status (verifierad 2026-09-15)

- **`main`:** `a772049` — "chore(release): changelog och README för v1.5.2" (fast-forward
  från v1.5.1, ingen separat release-commit skapades eftersom `main` var en strikt
  förfader till `develop`). `origin/main` matchar.
- **`develop`:** `15b9215` — "merge: bugfix/2026-015 (APP_VERSION 1.5.2) till develop",
  dvs. **ETT commit före `main`** (versionsfixen, se avsnitt 5, har ännu inte releasats).
  `origin/develop` matchar.
- **Taggar (webbappen, `v`-prefix):** tillägg sedan förra handoffen: **`v1.5.2`**
  (2026-09-15, FEAT-038). Övriga oförändrade (`v1.2.0` … `v1.5.1`), samtliga pushade.
- **Officiell URL:** https://peman68.github.io/PID-simulator/ — kör v1.5.2:s faktiska
  funktion (mätlinjerna), men UI:t visar felaktigt "v1.5.1" tills nästa release (se
  avsnitt 3/10).
- **Öppna lokala/fjärrbrancher:** `feature/regulatortrimning` (FEAT-031) — oförändrat
  sedan förra handoffen, fortfarande **inte mergad till `develop`**, väntar på
  PO/PM-beslut. Inga andra öppna feature-/bugfix-/release-brancher.
- **Arbetskatalogen vid skrivande stund:** `docs/exercises/ovningar-reglerstrategier.md`
  har en **okommitterad ändring** — PO:s pågående, för hand skrivna redigering av
  övningsdokumentet. **Rör inte den filen utan att fråga PO först** — den är mitt i en
  aktiv redigeringssession, inte ett misstag eller kvarglömd stash. (Den fixades förbi en
  gång under v1.5.2-releasen via `git stash`/`git stash pop` med PO:s uttryckliga
  tillstånd — se avsnitt 14 om du vill se hur.)

---

## 5. Genomförda uppdrag sedan HANDOFF_2026-09-10 (v1.5.1 → v1.5.2)

| Uppdrag | Syfte | Resultat / status | Rapport |
|---|---|---|---|
| **FEAT-037** | Nytt fristående övningsdokument "Reglerstrategier" (lösbladsform, ej lärstig) + facit | Skapat, itererat flera gånger: (1) initialt utkast, (2) facit-verifiering mot `sim-core.js` som hittade och rättade ett felaktigt K-värde och orealistiska pulsparametrar, (3) en full "slipningsomgång" av uppgiftstexten för nybörjare (explicita mätvärden att notera, konkreta acceptanskriterier, m.m.), (4) bytte Uppgift 1 Fall A från "Nivåprocess" till "Temperaturprocess" (kolliderade pedagogiskt med Uppgift 4:s nivå=integrerande-lektion), (5) byggde ut Uppgift 2 med en ny Fall C (Td-svep, simulatorverifierad: lagom Td≈1 eliminerar överslängen OCH mer än halverar äkta insvängningstid, med försumbar bruskostnad — men Td=3 gör allt sämre samtidigt), flyttade brus/D-dels-undersökningen dit från Uppgift 1, (6) upptäckte och rättade att HELA facit använt ett löst/inkonsekvent toleransband för "insvängningstid" — omräknat med ett enhetligt, dokumenterat 2 %-band över Uppgift 1, 2, 3, 4, 6, 7 (flera siffror ändrades påtagligt, t.ex. Uppgift 4:s PI-fall 385 s→167 s), (7) rättade att Uppgift 6 Fall B var räknad på fel pulsstorlek, (8) lade till ett "Termer och definitioner"-avsnitt i facit. **Status: fortfarande under PO:s granskning/manuella redigering — se avsnitt 8, punkt 1.** | `docs/exercises/ovningar-reglerstrategier.md`, `docs/exercises/facit-reglerstrategier.md`, `docs/tracking/todo.md` |
| **Bugg 2026-014** | PO:s fynd: "Återställ system" på en integrerande process fortsätter från föregående PV istället för normalvärdet | Registrerad med en misstänkt (ej verifierad) grundorsak från kodläsning. **Fortfarande öppen, ej felsökt i appen.** | `docs/tracking/buglog.md` |
| **FEAT-038** | Hjälplinjer 10 %/90 % (stigtid) och 2 %-toleransband i Mätläge, samt tunnare/transparenta hjälplinjer överlag | Implementerad i `feature/matlage-hjalplinjer`, mergad till `develop`, verifierad med Playwright (skärmdumpar, inga konsolfel) och `tests/build-preview.test.mjs`. Två öppna designval flaggade för PO (10/90 % som EN kryssruta, inte två separata; 2 % som ett band, inte en linje) — inget svar mottaget än, se avsnitt 8. | `docs/tracking/todo.md` (FEAT-038) |
| **RELEASE v1.5.2** | Publicera FEAT-038 till PROD | `develop` (som då även innehöll hela FEAT-037) mergad till `main`, taggad `v1.5.2`, pushad. `CHANGELOG.md`/`README.md` uppdaterade. Samtliga PROD-kontroller (`validate-content catalog.prod.json`, `analyze.test.mjs`, `build-preview.mjs prod`, `validate-prod.mjs`) gröna innan push. | `CHANGELOG.md`, `docs/tracking/todo.md` |
| **Bugg 2026-015** | PO:s fynd direkt efter release: appens UI visade "v1.5.1" trots att v1.5.2 var live | `APP_VERSION`-konstanten i `apps/app/app.js` glömdes bort i releaseprocessen. Rättad till `"1.5.2"` i `develop` (`bugfix/2026-015`, mergad). **Får effekt i PROD först vid NÄSTA release** — PO:s uttryckliga instruktion, ingen hotfix. PROD visar alltså just nu fel versionsnummer i UI:t. | `docs/tracking/buglog-done.md` |

---

## 6. Nuvarande lärstigar

**Oförändrat sedan HANDOFF_2026-09-10** — varken FEAT-037 eller FEAT-038 rörde
`catalog.json`/`catalog.prod.json` eller någon lärstigsfil.

**DEV** (`catalog.json`, 10 aktiva, katalogordning):
1. `kom-igång.v1` — **publicerad i PROD**
2. `oppen-slinga-onoff-p.v1` — **publicerad i PROD**
3. `proportionalband-forstarkning.v1` — **publicerad i PROD**
4. `pi-pid.v1` — **publicerad i PROD**
5. `processbegransningar.v1` — **publicerad i PROD**
6. `windup-antiwindup.v1` — **publicerad i PROD**
7. `storningar-robusthet.v1` — **publicerad i PROD**
8. `integrerande-process-niva.v1` — tekniskt klar, kvar i DEV, ej produktionsgodkänd
9. `stegsvar-identifiering.v1` — tekniskt klar, kvar i DEV, **har ett känt funktionsfel**
   (se avsnitt 8) — ska INTE produktionsgodkännas innan det är åtgärdat
10. `lambda-metoden.v1` — tekniskt klar, kvar i DEV, ett öppet pedagogiskt beslut väntar
    (se avsnitt 8)

**PROD** (`catalog.prod.json`): de sju markerade ovan, i exakt den ordningen.
14 beroendescenarier (13 fristående, `pi-deadtime-comparison` bara via
`processbegransningar.v1`), 6 teorimoduler.

---

## 7. Beslut som är fattade och genomförda

Utöver allt som redan var klart per HANDOFF_2026-09-10 (Nivåprogression i PROD,
DEV-konsolstöd, `showGamification`-frikoppling — se den filen om du behöver detaljerna,
fortfarande giltigt):

- **v1.5.2 är publicerad** — FEAT-038 (hjälplinjer i Mätläge) är live i PROD.
- **Facit-metodik för insvängningstid:** ett enhetligt, dokumenterat 2 %-toleransband
  (samma som `tests/simulation/lib/analyze.mjs`s standardvärde) används nu genomgående i
  `facit-reglerstrategier.md`, istället för det tidigare lösa/inkonsekventa bandet.
  PO-godkänt genom att uttryckligen be om "full konsekvens" när avvikelsen upptäcktes.
- **FEAT-038:s 2 %-linje är ett band, inte en enkel linje** — implementerat utan att
  invänta uttrycklig PO/PM-bekräftelse (den tekniskt korrekta tolkningen av
  "toleransband"), men flaggat i `todo.md` för eventuell korrigering.
- **FEAT-038:s 10 %/90 %-linjer styrs av EN gemensam kryssruta**, inte två separata —
  samma resonemang (de används alltid ihop som stigtidsdefinitionen), flaggat på samma
  sätt.
- **`APP_VERSION` ska rättas i NÄSTA release, inte som hotfix** — PO:s uttryckliga
  instruktion vid bugg 2026-015.
- **Facit-reglerstrategier.md ska renskrivas** (ingen historik, matcha övningsfilen
  exakt) när övningsfilen är färdigredigerad — registrerat som väntande TODO under
  FEAT-037, **görs INTE förrän PO uttryckligen säger att övningsfilen är klar.**

---

## 8. Beslut som INTE är fattade — öppna punkter

1. **FEAT-037 (`ovningar-reglerstrategier.md`) är under aktiv, pågående PO-redigering.**
   Vänta på ett uttryckligt "klar" innan du (a) rör den filen på eget initiativ, eller
   (b) påbörjar den registrerade facit-renskrivningen (avsnitt 7).
2. **Bugg 2026-014** (Återställ system-PV-bugg) är fortfarande öppen och ofelsökt i
   appen — bara en misstänkt grundorsak från kodläsning finns noterad. Kräver ett eget
   uppdrag: verifiera i appen, felsök, fixa, testa.
3. **FEAT-038:s två designval** (10/90 % som en gemensam kryssruta; 2 % som band) väntar
   på PO:s bekräftelse eller korrigering — se avsnitt 7. Om PO vill ha dem annorlunda är
   det en liten, avgränsad ändring i `apps/app/app.js`/`index.html`.
4. **`lambda-comparison`s pardragningslogik** — oförändrat sedan HANDOFF_2026-09-10,
   väntar fortfarande på PO/PM-beslut om det är värt ett eget uppdrag (se den filen för
   detaljer om inte redan bekant).
5. **Övriga två DEV-only-lärstigar** (`integrerande-process-niva.v1`, `lambda-metoden.v1`
   förutom punkt 4 ovan) väntar fortfarande på PO:s pedagogiska granskning innan
   produktionsgodkännande.
6. Rensning av föräldralösa lärstigsfiler (`grundlaggande.v1.json`, `windup.v1.json`,
   `basic-learning-path.v1.json`) — oförändrat olöst sedan tidigare handoffs.
7. Branschskydd på `main`, formella GitHub Release-poster — ingen `gh`-CLI tillgänglig
   (se avsnitt 10).
8. **FEAT-031** ("Regulatortrimning i praktiken", `feature/regulatortrimning`) —
   oförändrat sedan HANDOFF_2026-09-10, fortfarande inte mergad till `develop`. Nästa
   session bör kontrollera om den ska mergeas eller behöver mer arbete.

---

## 9. Mönster för produktionsgodkännande av en lärstig

Oförändrat sedan HANDOFF_2026-09-10 — se den filen för de fem stegen (inget nytt sedan
dess har testat eller ändrat detta mönster).

---

## 10. Kända tekniska förhållanden

Oförändrat sedan HANDOFF_2026-09-10 (två simuleringskärnor, statusraden, PB=100/Kp,
D-termen, `processbegransningar.v1`s kosmetiska id-mismatch, release-infrastrukturen,
PROD-artifaktens filtrering, gamification-arkitekturen — se den filen), **plus:**

- **`APP_VERSION` (rad 7 i `apps/app/app.js`) är en hårdkodad sträng, INTE automatiskt
  synkad med `CHANGELOG.md`/git-taggen.** Måste bumpas manuellt vid varje release — se
  bugg 2026-015 (avsnitt 5) och avsnitt 3. Föreslå gärna att lägga till en enkel kontroll
  i release-checklistan (jämför `APP_VERSION` mot `CHANGELOG.md`s senaste rubrik) så det
  inte glöms bort igen.
- **`tests/simulation/lib/analyze.mjs`s `runAnalysis()` har inget stöd för att trigga en
  pulsstörning** (`triggerPulse()` finns bara på `Simulation`-instansen, inte i
  analysbiblioteket) — pulstest måste skrivas som fristående skript direkt mot
  `tests/simulation/lib/sim-core-bridge.mjs` (se scratchpad-skripten från FEAT-037s
  verifieringsarbete om du behöver ett exempel; de sparades inte i repot).
- **Insvängningstid är känslig för toleransbandets bredd** — en aggressiv PI-inställning
  kan se "snabbt insvängd" ut vid ett löst band (t.ex. 5 %) bara för att dess egen
  översläng råkar ligga nära bandets gräns, trots att den i verkligheten fortsätter röra
  sig länge efteråt. Använd alltid samma, dokumenterade band (2 %,
  `tests/simulation/lib/analyze.mjs`s standardvärde) när du jämför strategier — se
  facit-reglerstrategier.md punkt 5 i "Viktigast" för ett konkret exempel på hur detta
  gav en missvisande slutsats tidigare.
- **Mätläget har nu fyra togglingsbara hjälplinjer** (63 %, 10 %/90 %, 2 %-band,
  tangentlinje) — samtliga `lineWidth = 1` + `globalAlpha = 0.6`, tydligt tunnare än
  PV/SP/u-kurvorna (`drawSeries()`, `lineWidth = 2`). Ny kod i `drawChart()` i
  `apps/app/app.js`, runt tidigare 63 %-linje-blocket.

---

## 11. Obligatorisk läsning för nästa session

1. **Detta dokument.**
2. `docs/exercises/ovningar-reglerstrategier.md` och `facit-reglerstrategier.md` — läs
   git-status FÖRST (`git status`), det kan finnas en okommitterad PO-redigering (se
   avsnitt 4).
3. `docs/tracking/todo.md` — särskilt FEAT-037-, FEAT-038- och RELEASE-relaterade poster
   (sök på "2026-09-14" och "2026-09-15" i filen för de senaste uppdateringarna).
4. `docs/tracking/buglog.md` — 2026-014 (öppen).
5. `docs/tracking/buglog-done.md` — 2026-015 (stängd i `develop`, väntar på release).
6. `CHANGELOG.md` — vad v1.5.2 faktiskt innehåller.
7. `apps/app/content/catalog.json` och `apps/app/content/catalog.prod.json` (oförändrade,
   men bra att bekräfta).

Om en sökväg skiljer sig när du läser detta, använd och dokumentera den faktiska
sökvägen istället för att gissa.

---

## 12. Nästa steg

**Väntar på PO/PM:**
- Ge besked när `ovningar-reglerstrategier.md` är färdigredigerad, så facit kan
  renskrivas (avsnitt 7) och lärstigsstatusen eventuellt omprövas.
- Bekräfta eller korrigera FEAT-038:s två designval (avsnitt 8, punkt 3).
- Prioritera felsökning av bugg 2026-014 (avsnitt 8, punkt 2) som ett eget uppdrag.
- Besluta om `lambda-comparison`s gemensam-referens-fråga (avsnitt 8, punkt 4).
- Bedöma om/när `integrerande-process-niva.v1` och `lambda-metoden.v1` ska
  produktionsgodkännas (avsnitt 8, punkt 5).
- Bedöma om `feature/regulatortrimning` (FEAT-031) ska mergeas till `develop` (avsnitt 8,
  punkt 8).
- Besluta om/när nästa release ska göras (för att få rätt `APP_VERSION` ut i PROD).

**Nästa session ska inte:**
- själv besluta att aktivera en lärstig i PROD utan uttryckligt PO/PM-uppdrag
- göra en ny release utan ett uttryckligt PO/PM-uppdrag
- ändra XP-värden, nivågränser eller `comparisonGroup` utan ett nytt uppdrag
- röra `ovningar-reglerstrategier.md`/`facit-reglerstrategier.md` på eget initiativ om
  git status visar en okommitterad ändring — fråga PO först
- anta att gamification fortfarande är DEV-only — den är produktionsaktiv sedan v1.5.0

---

## 13. Startkontroll på den andra datorn

1. Kör `git fetch origin`.
2. Kontrollera aktuell branch, `git status`, att arbetskatalogen är ren **eller** att en
   ev. okommitterad ändring i `ovningar-reglerstrategier.md` är PO:s pågående arbete
   (inte något att kasta).
3. Byt till `develop`, kör `git pull --ff-only origin develop`.
4. Läs `docs/handoffs/HANDOFF_2026-09-15.md` (denna fil).
5. Kör:
   ```bash
   node tests/validate-content.mjs
   node tests/validate-content.mjs catalog.prod.json
   node tests/simulation/analyze.test.mjs
   node tests/activity-prototype.test.mjs
   node tests/gamification/xp-model.test.mjs
   node tests/gamification/gamification-engine.test.mjs
   node tests/gamification/gamification-store.test.mjs
   node tests/gamification/gamification-dev-prod-isolation.test.mjs
   node tests/gamification/gamification-session-finalization.test.mjs
   node tests/gamification/gamification-reset-isolation.test.mjs
   node tests/gamification/comparison-groups-content.test.mjs
   node tests/build-preview.test.mjs
   node tests/build-preview.mjs prod
   node tests/validate-prod.mjs
   ```
6. Rapportera branch, commit, testresultat.
7. Vänta därefter på konkret uppdrag.

**CC ska inte starta apputveckling bara för att startkontrollen passerar.**

---

## 14. Sessionsspecifik kontext

### FEAT-037 — ett iterativt, flerstegs granskningsarbete

Denna session bestod till stor del av upprepade granskningsrundor på
`ovningar-reglerstrategier.md`/`facit-reglerstrategier.md`, där PO gav uppdrag löpande
och själv redigerade filen parallellt i sin IDE. Viktiga lärdomar:

- **PO redigerar filer direkt, samtidigt som CC jobbar på dem.** Flera gånger under
  sessionen ändrades `ovningar-reglerstrategier.md` "på disk" mellan CC:s verktygsanrop
  (syns som "filen har ändrats sedan du senast läste den"-varningar). Rätt hantering:
  läs om filen, ta PO:s ändring som avsiktlig, bygg vidare på den — flagga bara om
  ändringen ser felaktig ut, annars inget att kommentera.
- **När en verklig konflikt uppstod** (CC behövde växla bort från `develop` för att göra
  releasen, men `ovningar-reglerstrategier.md` hade en okommitterad PO-redigering och
  filen existerar inte alls på `main`) blockerade auto-mode-klassificeraren `git stash`
  som "irreversible local destruction" trots att stash är fullt återställbart. CC
  stannade och frågade PO om lov — PO godkände, CC stashade, gjorde releasen, växlade
  tillbaka och poppade stashen. Om du hamnar i samma situation: fråga, gör inte
  antaganden om att en autentisk fyllydsig destruktiv-klassad git-operation är okej bara
  för att den tekniskt är reversibel.
- **Facit-verifiering avslöjade ett metodproblem, inte bara enskilda felaktiga tal:**
  ett odokumenterat, inkonsekvent toleransband för "insvängningstid" hade smugit sig in
  över flera uppgifter. Lärdomen generaliserar: när du verifierar pedagogiskt facit mot
  en simulator, kontrollera ALLTID vilken exakt definition/tolerans som användes för
  varje mätvärde, och håll den konsekvent — annars kan en "snabbare" inställning se
  bättre ut bara för att den råkar passera en generös tröskel tidigt, inte för att den
  faktiskt är bättre.

### FEAT-038 — implementerad och släppt samma session

Ovanligt snabb cykel: registrerad som TODO → PO bad om implementation → implementerad i
en feature-branch → verifierad med Playwright mot en lokal `python -m http.server` →
mergad till `develop` → PO bad om release → releasad som v1.5.2 samma session. Ingen
staging-branch (`test/<kortnamn>`) användes — matchar det redan etablerade, enklare
mönstret från tidigare releaser (se HANDOFF_2026-09-10 om FEAT-035/RELEASE-v1.5.0).

### Release v1.5.2 — releasechecklistan missade `APP_VERSION`

Se avsnitt 3/5/10. Kom ihåg detta vid NÄSTA release: uppdatera `apps/app/app.js`s
`APP_VERSION`-konstant, inte bara `CHANGELOG.md`/`README.md`. Fixen finns redan i
`develop` (bugg 2026-015) och kommer med automatiskt nästa gång — ingen separat åtgärd
krävs förutom att komma ihåg att en release faktiskt behöver göras för att den ska synas
i PROD.

### Verktyg som användes men inte sparades i repot

- Ett antal engångs-Node-skript för att verifiera facit-siffror mot `sim-core.js`
  (Td-svep, bruskänslighet/`u_std`, pulstest via `triggerPulse()`) sparades i sessionens
  scratchpad, inte i repot. Om nästa session behöver liknande verifiering, fråga PO om
  det är värt att göra dem till ett återanvändbart verktyg i `tests/`.
- Playwright (via `npx playwright`, cachad Chromium) användes för att visuellt verifiera
  FEAT-038 i en riktig webbläsare. Ingen `chromium-cli` fanns tillgänglig i miljön. Om du
  behöver göra samma sak: `npx playwright --version` för att bekräfta cache, importera
  paketet via en direkt filsökväg till `.../node_modules/playwright/index.mjs` eftersom
  `NODE_PATH` inte fungerar för ESM-imports av bara namn.

### Verifierat 2026-09-15

- `git stash list` — tom (poppad efter release).
- `main` (`a772049`) och `develop` (`15b9215`, ett commit före `main`) matchar
  `origin/main`/`origin/develop` exakt.
- Ingen `gh`-CLI eller annan autentiserad GitHub-åtkomst fanns tillgänglig, oförändrat.
